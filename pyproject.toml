# pip install -e .
[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "shandalar-tools"
version = "2.0.0.dev0"

[tool.setuptools.packages.find]
where = ["src"]